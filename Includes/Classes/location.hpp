#ifndef LOCATION_HPP
# define LOCATION_HPP

# include <string>

class Location {
    public:
    private:
        std::string path;
        std::string root;
        std::string index;
        std::string client_max_body_size;
       
        // methods
        // dir_listing
        // cgi
};

#endif // LOCATION_HPP